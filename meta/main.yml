---
galaxy_info:
  name: chroot_tmpfs_raw

  author: Jake Smith
  description: Ansible role creating a tmpfs chroot and installing packages into it using raw module

  min_ansible_version: 2.7

  galaxy_tags:
    - chroot
    - tmpfs
    - raw

dependencies: []